# OS / editor junk
.DS_Store
**/.DS_Store

# LibreOffice / office lock files
.~lock.*#
**/.~lock.*#

# Sources de travail non publiées (contiennent des métadonnées privées,
# ex. le vrai nom de l'auteur dans les .pptx) — ne jamais publier.
a_traiter/

# Outils / plans de l'agent (pas du contenu du site)
.pi/

# Dépôt git imbriqué (projet séparé, publié sur ses propres Pages) — ne pas embarquer
empire-calendrier/

# Catalogues bruts téléchargés pour l'atlas exoplanètes (NASA/PHL/exoplanet.eu) :
# données de travail volumineuses, NON servies par le site. La page charge uniquement
# data/atlas-data.js (consolidé). Régénérables via les API NASA Exoplanet Archive / PHL.
provoxys/exoplanetes/data/*.csv
# Notes de travail de rédaction (non publiées)
provoxys/exoplanetes/script.md
